# 05 技術スタック（エンジン選定・構成）

最終更新: 2026-06-18
状態: エンジン決定（Godot 4）。プロジェクト構成・配信は着手時に具体化。

## 決定サマリ

| 項目 | 決定 | 備考 |
|---|---|---|
| ゲームエンジン | **Godot 4**（最新安定版） | 無料・2D本職・Windows/Steam書き出し容易 |
| スクリプト言語 | GDScript（基本） | 必要箇所のみ C#/GDExtension を検討（当面は不要見込み） |
| 対象プラットフォーム | PC / Windows（Steam・itch 等） | 配信先確定は roadmap で |
| バージョン管理 | git（リモート: GitHub `5001_KeyInputGame`） | R: ドライブの git 癖対応済み |

## ADR-001: ゲームエンジンに Godot 4 を採用

- **状況**: PC（ダウンロード/Steam）向けの 2D グリッド型リアルタイムアクション。操作はキーボードのみ。
  シンプル基調・個人開発・ステージ制で拡張、という要件。
- **決定**: **Godot 4** を採用する。
- **理由**:
  - 無料・オープンソースでライセンス課金や規約変更リスクがない（個人開発・シンプル基調と整合）。
  - 2D が本職で、グリッド型2Dアクションに向く。軽量でイテレーションが速い。
  - Windows 書き出しが容易、Steam 連携（Steamworks アドオン）も実績あり。
  - 入力系（InputMap）が扱いやすく、キー割り当て・リマップが標準機能。**キーボードのみ**というコア制約と直結。
- **却下案**:
  - **Unity**: 資産・情報は豊富だが重め・C#・ライセンス考慮が必要で、今回の規模にはオーバースペック。
  - **Web系（Phaser/TS）**: キーボードと好相性だが、Steam ネイティブ配信には Electron 等のラップが必要で重くなる。
- **影響**: 以降のプロジェクト構成・入力定義（06_systems）・配信（07_roadmap）は Godot 4 前提で書く。

## アートディレクション（→ 08_world-vision）

- **HD-2D 風**: ドット絵（2Dスプライト）を **3D空間**に配置するスタイル（オクトパストラベラー系）。
  - Godot 4 は 3D空間に 2D スプライト（`Sprite3D`）を置けるため実現可能。戦闘の 3×10 グリッドも 3D アリーナ＋スプライトで表現できる。
  - ただし HD-2D 調のライティング/被写界深度の作り込みは手間。**まず簡易な 2.5D 表現から始め**、見栄えは後で寄せる。
- **アセットは全て AI 生成予定**（開発者は作画・モデリングをしない）。
  - リスク: **画風・サイズ・向き・パレットの一貫性**。スプライトシート/アニメの規格が割れると破綻する。
  - 対策: **早い段階で小さく検証**（数体のキャラ＋背景1枚で、グリッド上に並べて違和感が無いか）。生成規格（解像度・余白・8方向有無）を先に決める。
  - 純 3D モデルを AI で安定生成するのは現状ハードルが高い。**スプライト主体**の方が AI 生成と相性が良い見込み。

## プロジェクト構成（着手時に確定）

- Godot プロジェクト（`project.godot`）をリポジトリ直下 or `game/` 配下に作成（着手時に決定）。
- シーン分割・疎結合（直接参照しない）を最初から：シーン間はシグナル／オートロード（共有データ・サービス）経由。
- 入力はキー直書きせず **InputMap のアクション名**で参照（リマップ・コア制約の担保）。

## 拠点依存（家／会社）

- **Godot 本体のパスは拠点で異なる**（→ [environment.md](../../5000_GamePhilosohy/environment.md)）。
  絶対パスを埋め込まず、ビルド・実行の前にその場で確認する。
- **要確認（会社）**: この拠点の Godot 4 実行ファイルのパス／バージョン。確定したら environment.md の表に追記。

## 要確認

- Godot プロジェクトの配置（直下か `game/` か）。
- Godot のバージョン（4.x のどれで固定するか）。
- Steam 配信の有無と時期（roadmap）。
